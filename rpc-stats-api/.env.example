# Port the API listens on
PORT=3003

# How often to scrape all nodes (milliseconds)
SCRAPE_INTERVAL=5000

# CORS — set to your frontend domain in production
# e.g. ALLOWED_ORIGINS=https://rpc.yourdomain.com
ALLOWED_ORIGINS=*
